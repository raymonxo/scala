import math._
import math.BigInt._
import scala.util._

object worksheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(87); 

  val x = 3;System.out.println("""x  : Int = """ + $show(x ));$skip(12); 
  var r = 2;System.out.println("""r  : Int = """ + $show(r ));$skip(35); 
  if (x > 0) { r = r * x; r -= 1 };$skip(4); val res$0 = 
  r;System.out.println("""res0: Int = """ + $show(res$0))}
}
