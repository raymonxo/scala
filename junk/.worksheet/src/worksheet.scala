import math._
import math.BigInt._
import scala.util._
import scala.collection.mutable.ArrayBuffer

object worksheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(129); val res$0 = 

  (1,2);System.out.println("""res0: (Int, Int) = """ + $show(res$0))}
  
}
